# Анализ мёртвого кода

## A) СТРАНИЦЫ (Pages)

### ✅ К удалению:
1. **dashboard.tsx**
   - НЕ импортируется в App.tsx
   - НЕ используется в роутере
   - НЕ имеет ссылок в навигации
   - Доказательство: только определение компонента, нет импортов

## B) КОМПОНЕНТЫ UI

### ✅ К удалению:
1. **aspect-ratio.tsx**
   - НЕ импортируется нигде (только определение)
   
2. **avatar.tsx**
   - НЕ импортируется нигде (только определение)
   
3. **breadcrumb.tsx**
   - НЕ импортируется нигде (только определение)
   
4. **collapsible.tsx**
   - НЕ импортируется нигде (только определение)
   
5. **hover-card.tsx**
   - НЕ импортируется нигде (только определение)
   
6. **pagination.tsx**
   - НЕ импортируется нигде (только определение)
   
7. **scroll-area.tsx**
   - НЕ импортируется нигде (только определение)
   
8. **tabs.tsx**
   - НЕ импортируется нигде (только определение)
   
9. **chart.tsx**
   - НЕ импортируется нигде (только определение)
   - Используется recharts, но компонент не используется

### ✅ Используются (НЕ удалять):
- accordion.tsx - используется в settings.tsx
- sheet.tsx - используется в sidebar.tsx

## C) ХУКИ И УТИЛИТЫ

### ✅ Проверить:
- use-mobile.tsx - используется в sidebar.tsx
- use-toast.ts - используется везде
- youtube.ts - используется в нескольких местах
- utils.ts - используется везде

## D) БЭКЕНД API

### ✅ К удалению:
1. **GET /api/statistics**
   - НЕ вызывается с фронта
   - Страница statistics удалена
   
2. **PUT /api/languages/reorder**
   - НЕ вызывается с фронта
   - Drag-and-drop удален из languages.tsx

### ✅ Проверить:
- PUT /api/languages - используется ли? (старый способ reorder)

## E) ЗАВИСИМОСТИ

### ✅ К удалению (если компоненты удалены):
- @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities - НЕ используются
- recharts - НЕ используется (chart.tsx не используется)
- next-themes - НЕ используется (тема только dark)
- @radix-ui/react-aspect-ratio - НЕ используется
- @radix-ui/react-avatar - НЕ используется
- @radix-ui/react-breadcrumb - НЕ используется (если есть)
- @radix-ui/react-collapsible - НЕ используется
- @radix-ui/react-hover-card - НЕ используется
- @radix-ui/react-scroll-area - НЕ используется
- @radix-ui/react-tabs - НЕ используется

